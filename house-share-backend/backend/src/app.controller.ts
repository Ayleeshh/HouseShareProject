// A Schema is a blueprint for how data is stored in MongoDB.
// A DTO is basically a blueprint that defines what data is allowed into your API.
// Controllers — receive the incoming requests.
// Services — run the actual logic.
// Modules — group related features together